import requests

def process_data(data):
    # read data from input
    # name = request.args.get('name', "John")
    # admission = request.args.get('admission', "Heart Failure")
    # medication = request.args.get('medication', [])
    # appointments = request.args.get('appointments', "")
    # upload record to the database
    return